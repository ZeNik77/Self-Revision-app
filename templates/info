        
                <!-- 
                <div class="btn-group">
                    <button type="button" onclick="enableEdit({{ c.id }})" class="btn btn-sm btn-light">✏️</button>

                    <form method="post" class="d-inline ms-1" onsubmit="return confirmDelete();">
                        {% csrf_token %}
                        <button type="submit" name="delete_course" value="{{ c.id }}" class="btn btn-sm btn-light">🗑️</button>
                    </form>
                </div>

                <form method="post" id="form-{{ c.id }}" class="d-none">
                    {% csrf_token %}
                    <div class="mb-2">
                        <input type="text" name="name" class="form-control" value="{{ c.name }}">
                    </div>
                    <div class="mb-2">
                        <textarea name="description" class="form-control" value="{{ c.description }}">{{ c.description }}</textarea>
                    </div>
                    <button type="submit" name="edit_course" value="{{ c.id }}" class="btn btn-sm btn-success">💾 Save</button>
                </form> -->

        <!-- <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
                <a href="{% url 'course' c.course_id %}" class="nav-link text-white">
                <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                    <h5 class="mb-0 me-3" id="title-{{ c.id }}">{{ c.name }}</h5>

                    <div class="btn-group">
                        <button type="button" onclick="enableEdit({{ c.id }})" class="btn btn-sm btn-light">✏️</button>
                        <form method="post" class="d-inline ms-1" onsubmit="return confirmDelete();">
                            {% csrf_token %}
                            <button type="submit" name="delete_course" value="{{ c.id }}" class="btn btn-sm btn-light">🗑️</button>
                        </form>
                    </div>
                </div>
                </a>

                <div class="card-body">
                    <p class="card-text" id="desc-{{ c.id }}">{{ c.description }}</p>

                    <form method="post" id="form-{{ c.id }}" class="d-none">
                        {% csrf_token %}
                        <div class="mb-2">
                            <input type="text" name="name" class="form-control" value="{{ c.name }}">
                        </div>
                        <div class="mb-2">
                            <textarea name="description" class="form-control" value="{{ c.description }}">{{ c.description }}</textarea>
                        </div>
                        <button type="submit" name="edit_course" value="{{ c.id }}" class="btn btn-sm btn-success">💾 Save</button>
                    </form>
                </div>
            </div>
        </div> -->
        <!-- <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
                <div class="row justify-content-center">
                    <div class="col-md-12">
                        <div class="card shadow-sm">
                            <div class="card-header bg-primary text-white">
                                <h4 class="mb-0">Add New Course</h4>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    <form method="POST">
                        {% csrf_token %}
                        {{ form.non_field_errors }}

                        <div class="mb-1">
                            {{ form.name }}
                            {% if form.name.errors %}
                                <div class="text-danger">{{ form.name.errors.0 }}</div>
                            {% endif %}
                        </div>

                        <div class="mb-1">
                            {{ form.description }}
                            {% if form.description.errors %}
                                <div class="text-danger">{{ form.description.errors.0 }}</div>
                            {% endif %}
                        </div>

                        <button type="submit" name="add_course" class="btn btn-success">Add Course</button>
                    </form>
                </div>
            </div>
        </div> -->
